# SonarLint S2245 Fixes - Math.random() → secureRandom()

## Steps
- [x] Create TODO tracking file
- [ ] Add `secureRandom()` helper using `crypto.getRandomValues()`
- [ ] Replace all `Math.random()` calls with `secureRandom()` in `script.js`
- [ ] Verify no runtime errors

