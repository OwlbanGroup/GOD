# GOD Server Setup - Task Tracker

## Goal
Get the GOD Node/Express + Socket.IO server running locally and verify `/health` returns 200.

## Steps
- [ ] 1. Run `npm install --no-audit --no-fund` in `C:\Users\bsean\Desktop\GOD` to ensure dependencies are complete/consistent
- [ ] 2. Verify `cors` loads correctly (`node -e "require('cors')"`)
- [ ] 3. Start the server in background and verify `/health` returns 200
- [ ] 4. Update `GOD/TODO_SETUP.md` (mark steps 5 & 6 complete)
- [ ] 5. Update `GOD/TODO_RUN_SEAMLESS.md` (mark GOD steps complete)
